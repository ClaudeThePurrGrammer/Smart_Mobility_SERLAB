# IP della macchina host (usato da Metro per pubblicizzarsi su LAN)
# Trovalo con: ipconfig (Windows) oppure ifconfig (macOS/Linux)
HOST_IP=192.168.x.x

# Sprint 2 — Backend
# JWT_SECRET=cambia_questo_valore_in_produzione
